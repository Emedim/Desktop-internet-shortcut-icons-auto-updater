{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug C (MSVC)",
      "type": "cppvsdbg",
      "request": "launch",

      "program": "${workspaceFolder}\\bin\\DISIAU.exe",
      "cwd": "${workspaceFolder}\\bin",  //рабочая директория процесса

      "args": [],             // параметры главной функции отлаживаемой программы
      "stopAtEntry": false,   // остановиться ли сразу с первой строчки кода при отладке

      "preLaunchTask": "build",

      "console": "externalTerminal"
    }
  ]
}
